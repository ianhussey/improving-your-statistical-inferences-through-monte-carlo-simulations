*.Rproj.user
*.Rhistory
*.RData
*.Ruserdata
*.DS_Store
*.rds
.claude/*

/.quarto/
old/*
_book/*

# quarto per-document support files (figures, libs) and knitr chunk caches.
# the trailing slash restricts these to directories.
*_files/
*_cache/

# claude code git worktrees. these are throwaway copies of the whole repo, so
# committing them duplicates the entire book inside itself
.claude/worktrees/

**/*.quarto_ipynb
*.history
